<div class="well">
  <h3>Ваш маршрут</h3>
  <% from, *through, to = places %>
  <table class="table table-striped table-bordered">
    <tr>
      <td><strong>Откуда</strong></td>
      <td><%= "#{from.city}, #{from.street}, #{from.house}"%></td>
      <td><i class="icon-pencil icon-blue"></i></td>
      <td><i class="icon-remove icon-red"></i></td>
    </tr>
    <% if through.any? %>
      <% through.each do |place| %>
        <tr>
          <td><strong>Через</strong></td>
          <td><%= "#{place.city}, #{place.street}, #{place.house}"%></td>
          <td><i class="icon-pencil icon-blue"></i></td>
          <td><i class="icon-remove icon-red"></i></td>
        </tr>
      <% end %>
    <% end %>
    <%if to.present? %>
      <tr>
        <td><strong>Куда</strong></td>
        <td><%= "#{to.city}, #{to.street}, #{to.house}"%></td>
        <td><i class="icon-pencil"></i></td>
        <td><i class="icon-remove"></i></td>
      </tr>
    <% end %>
  </table>
</div>
